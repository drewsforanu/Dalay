#include <iostream>
using namespace std;

int factorial(int n) {
    return (n == 0 || n == 1) ? 1 : n * factorial(n - 1);
}

int main() {
    int n;
    cout << "请输入一个正整数: ";
    cin >> n;
    cout << n << " 的阶乘是: " << factorial(n) << endl;
    return 0;
}
