import random

number = random.randint(1, 100)
print("猜一个 1 到 100 之间的数字：")

while True:
    guess = int(input("你的猜测: "))
    if guess < number:
        print("太小了！")
    elif guess > number:
        print("太大了！")
    else:
        print("恭喜，你猜对了！")
        break
